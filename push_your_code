#this script to push your code on GitHub
#you need to run the clone_to_GitHub script first 

if [ "$2" == "false" ]; then
    chmod 764 "$2"
elif [ "$2" == "true" ]; then
    continue
fi

git add .
git commit -m "$1"
git push
